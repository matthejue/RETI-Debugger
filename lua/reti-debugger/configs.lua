local default_options = {
  keys = {
    next = "n",
    switch_window = "<tab>",
    switch_window_backwards = "<s-tab>",
    switch_mode = "m",
    refocus_memory = "r",
    -- load_example = "<leader>pl",
    -- compile = "<leader>pc",
    -- start = "<leader>ps",
    -- restart = "<leader>pr",
    -- hide = "<leader>ph",
    quit = "q"
  }
}

return default_options
