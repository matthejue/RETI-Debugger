local default_options = {
  keys = {
    next = "n",
    switch_window = "<tab>",
    switch_window_backwards = "<s-tab>",
    switch_mode = "m",
    focus_memory = "f",
    -- load_example = "<leader>pl",
    -- compile = "<leader>pc",
    -- start = "<leader>ps",
    -- hide = "<leader>ph",
    restart = "r",
    quit = "q"
  }
}

return default_options
