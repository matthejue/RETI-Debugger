local default_options = {
  keys = {
    next = "n",
    switch_window = "<tab>",
    switch_window_backwards = "<s-tab>",
    switch_mode = "m",
    refocus_memory = "r",
    -- hide = "<leader>ph",
    -- start = "<leader>ps",
    -- restart = "<leader>pr",
    -- compile = "<leader>pc",
    quit = "q"
  }
}

return default_options
